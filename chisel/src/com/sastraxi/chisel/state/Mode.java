package com.sastraxi.chisel.state;

/**
 * A mode defines how the user interacts with a View: what can be clicked on,
 * what clicking on those things does. It also defines how the View is
 * rendered.
 *
 * For example, one Mode might be "vertex editing" and one might be "camera
 * moving" and one might be "texture modification". All of these require
 * slightly-different approaches to rendering and object selection (faces vs.
 * vertices, for example).
 */
public abstract class Mode {

    // can get focus?
    // key + mouse listener

}
